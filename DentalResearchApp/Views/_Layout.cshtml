@using DentalResearchApp.Models
@using Microsoft.AspNetCore.Rewrite.Internal.UrlActions
<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>@ViewBag.Title</title>

    <link rel="stylesheet" href="/css/Index.css">

   </head>
<body>
    <div class="wrapper">
        <nav id="sidebar">
            <div class="" style="text-align: center">

                <h4>@Html.ActionLink("Department of Dentistry and Oral Health, Aarhus", "AllStudies", "Study")</h4>
            </div>



                <li>
                    <a href="#surveySubmenu" data-toggle="collapse" aria-expanded="false" class="dropdown-toggle">Surveys <span class="caret"></span></a>
                    <ul class="collapse list-unstyled" id="surveySubmenu">
                        <li>
                            @Html.ActionLink("Survey Editor", "Index", "Survey")
                            @Html.ActionLink("Send Survey", "SendSurvey", "Link")
                            @Html.ActionLink("All Results", "SurveyResults", "Survey")
                        </li>
                    </ul>
                </li>
            </ul>

            <hr />
            <ul class="list-unstyled">
                <li>
                    @Html.ActionLink("Studies", "AllStudies", "Study")
                </li>
                <li>

                    @Html.ActionLink("Find Results", "FindResults", "Survey")
                </li>
            </ul>

            <hr />
            <ul class="list-unstyled">
                <li>
                    @Html.ActionLink("Profile", "Profile", "Profile")
                </li>
            </ul>   
            <ul class="list-unstyled">
                <li>
                    @Html.ActionLink("Logout", "Logout", "Login")
                </li>
            </ul>
        </nav>
        <div id="content" style="width:inherit; height:inherit">
            @RenderBody()
        </div>
    </div>
</body>
</html>
